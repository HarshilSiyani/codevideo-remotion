/**
 * Custom Hooks Index
 *
 * Export all custom hooks for easy importing
 */
export {
  useFade,
  useSlide,
  useSpringAnimation,
  useScale,
  useProgress,
  useTime,
  useLoop,
  usePulse,
  useDelayedAnimation,
} from "./useAnimation";

export type {
  FadeOptions,
  SlideOptions,
  SpringOptions,
  ScaleOptions,
} from "./useAnimation";
